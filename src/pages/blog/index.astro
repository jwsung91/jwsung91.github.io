---
import { getCollection } from 'astro:content';
import PostMetaBadges from '../../components/PostMetaBadges.astro';
import BaseLayout from '../../layouts/BaseLayout.astro';
import {
  formatDate,
  getBlogPath,
  getProjectPosts,
  getSitePosts,
  getStudyPosts,
  groupPostsByTopic,
  sortBlogPosts,
  topicLabels,
  type BlogProject,
} from '../../lib/blog';

const allPosts = await getCollection('blog');
const posts = sortBlogPosts(
  allPosts.filter((post) => post.data.draft !== true),
);
const projectGroups = (['unilink', 'ai-curator'] as BlogProject[])
  .map((project) => ({
    project,
    posts: getProjectPosts(posts, project),
  }))
  .filter((group) => group.posts.length > 0);
const studyGroups = [...groupPostsByTopic(getStudyPosts(posts)).entries()];
const sitePosts = getSitePosts(posts);
const projectDocTitles: Record<BlogProject, string> = {
  unilink: 'unilink 설계 노트',
  'ai-curator': 'AI Curator 파이프라인 구축기',
  site: 'Site Notes',
};
---

<BaseLayout
  title="Blog — jwsung91"
  description="Writing about systems, robotics, and software architecture."
>
  <main class="page-layout">
    <section class="mb-16">
      <h1
        class="mb-3 text-3xl font-bold text-zinc-900 dark:text-zinc-100 md:text-4xl"
      >
        Writing
      </h1>
      <p class="text-base leading-relaxed text-zinc-600 dark:text-zinc-400">
        프로젝트와 개인 학습을 기록하는 기술 문서 아카이브입니다.
      </p>
    </section>

    <section class="mb-16">
      <h2 class="mb-6 font-mono text-xs uppercase tracking-wider text-zinc-500">
        Project Docs
      </h2>
      <div class="grid gap-4 md:grid-cols-2">
        {
          projectGroups.map((group) => {
            const firstPost = group.posts[group.posts.length - 1];

            return (
              <article class="rounded-lg border border-zinc-200 bg-zinc-100/50 p-5 dark:border-zinc-800 dark:bg-zinc-900/50">
                <h3 class="text-lg font-semibold text-zinc-900 dark:text-zinc-100">
                  {projectDocTitles[group.project]}
                </h3>
                <p class="mt-2 text-sm leading-relaxed text-zinc-600 dark:text-zinc-400">
                  {group.posts.length}개의 프로젝트 문서가 있습니다.
                </p>
                <a
                  href={`/projects/${group.project}/`}
                  class="mt-4 inline-block font-mono text-sm text-coral-600 dark:text-coral-400 hover:underline"
                >
                  project docs →
                </a>
                {firstPost && (
                  <a
                    href={getBlogPath(firstPost)}
                    class="mt-2 block text-sm text-zinc-600 hover:text-zinc-900 dark:text-zinc-400 dark:hover:text-zinc-100"
                  >
                    시작하기: {firstPost.data.title}
                  </a>
                )}
              </article>
            );
          })
        }
      </div>
    </section>

    <section class="mb-16">
      <h2 class="mb-6 font-mono text-xs uppercase tracking-wider text-zinc-500">
        Study Notes
      </h2>
      {
        studyGroups.length > 0 ? (
          <div class="grid gap-4 md:grid-cols-2">
            {studyGroups.map(([topic, topicPosts]) => (
              <article class="rounded-lg border border-zinc-200 bg-zinc-100/50 p-5 dark:border-zinc-800 dark:bg-zinc-900/50">
                <h3 class="text-lg font-semibold text-zinc-900 dark:text-zinc-100">
                  {topicLabels[topic]}
                </h3>
                <p class="mt-2 text-sm leading-relaxed text-zinc-600 dark:text-zinc-400">
                  {topicPosts.length}개의 학습 기록이 있습니다.
                </p>
                <a
                  href={getBlogPath(topicPosts[0])}
                  class="mt-4 inline-block font-mono text-sm text-coral-600 dark:text-coral-400 hover:underline"
                >
                  latest study →
                </a>
              </article>
            ))}
          </div>
        ) : (
          <p class="text-base leading-relaxed text-zinc-600 dark:text-zinc-400">
            공개된 study 글이 아직 없습니다.
          </p>
        )
      }
    </section>

    <section class="mb-16">
      <h2 class="mb-6 font-mono text-xs uppercase tracking-wider text-zinc-500">
        Site Notes
      </h2>
      {
        sitePosts.length > 0 ? (
          <ul class="space-y-3">
            {sitePosts.map((post) => (
              <li>
                <a
                  href={getBlogPath(post)}
                  class="group block rounded-lg border border-zinc-200 bg-zinc-100/50 p-5 dark:border-zinc-800 dark:bg-zinc-900/50"
                >
                  <div class="mb-3 flex flex-wrap items-center gap-3">
                    <span class="font-mono text-xs uppercase tracking-wider text-zinc-500">
                      {formatDate(post.data.date)}
                    </span>
                    <PostMetaBadges
                      project={post.data.project}
                      kind={post.data.kind}
                      topic={post.data.topic}
                    />
                  </div>
                  <h3 class="text-lg font-semibold text-zinc-900 transition-colors duration-200 group-hover:text-zinc-700 dark:text-zinc-100 dark:group-hover:text-zinc-300">
                    {post.data.title}
                  </h3>
                  <p class="mt-2 text-sm leading-relaxed text-zinc-600 dark:text-zinc-400">
                    {post.data.description}
                  </p>
                </a>
              </li>
            ))}
          </ul>
        ) : (
          <p class="text-base leading-relaxed text-zinc-600 dark:text-zinc-400">
            공개된 site note가 아직 없습니다.
          </p>
        )
      }
    </section>

    <section>
      <h2 class="mb-6 font-mono text-xs uppercase tracking-wider text-zinc-500">
        Latest
      </h2>
      <ul>
        {
          posts.map((post) => (
            <li class="border-b border-zinc-200 dark:border-zinc-800 py-6 first:pt-0 last:border-b-0 last:pb-0">
              <a href={getBlogPath(post)} class="group block">
                <div class="mb-3 flex flex-wrap items-center gap-3">
                  <span class="font-mono text-xs uppercase tracking-wider text-zinc-500">
                    {formatDate(post.data.date)}
                  </span>
                  <PostMetaBadges
                    project={post.data.project}
                    kind={post.data.kind}
                    topic={post.data.topic}
                  />
                </div>
                <h2 class="text-xl font-semibold text-zinc-900 dark:text-zinc-100 transition-colors duration-200 group-hover:text-zinc-700 dark:group-hover:text-zinc-300 md:text-2xl">
                  {post.data.title}
                </h2>
                {post.data.description && (
                  <p class="mt-2 text-base leading-relaxed text-zinc-600 dark:text-zinc-400">
                    {post.data.description}
                  </p>
                )}
              </a>
            </li>
          ))
        }
      </ul>
    </section>
  </main>
</BaseLayout>
